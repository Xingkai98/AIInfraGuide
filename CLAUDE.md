## Agent skills

### Issue tracker

Issues live as GitHub issues on the personal fork `Xingkai98/AIInfraGuide` (never upstream). See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical roles use their default strings (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

## 当前工作

交互学习实验室（interactive learning labs）。设计文档在 `docs/plans/interactive-labs.md`。
路线图与任务拆分以 GitHub Issues 上的 wayfinder map 为准（见 `docs/agents/issue-tracker.md` 的 Wayfinding operations）。
