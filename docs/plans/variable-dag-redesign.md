# 变量图改造：L00 试点

> 状态：**L00 试点已完成并合并**（commit `42ccbac`）。其余 6 个 lab 待定。用户 2026-09-26 给出的标准，以及对照现状的差距实测。

## 用户要的形态（原话拆解）

| # | 要求 | 现状 | 差距 |
|---|---|---|---|
| 1 | **每个知识点基本一屏** | 2.9–9.4 屏（L00 2.9、L06 4.7、L05 7.9、L04 9.4） | ❌ 超 3–9 倍 |
| 2 | **DAG 节点是变量** | 节点是**步骤**（`m1` = "第1块:更新最大值"） | ❌ 语义相反 |
| 3 | **边是一次计算** | 边是"某张量流向某步骤" | ❌ 语义相反 |
| 4 | **数据结构是二维矩阵** | 有张量检查器（网格+热力图），但不在图里 | ⚠️ 部分 |
| 5 | **单步进退 + 当前步高亮** | 已有（播放/单步/时间轴/跳转） | ✅ 符合 |
| 6 | **侧栏 LaTeX 公式** | 已有（三档：符号/索引/数值） | ✅ 符合 |
| 7 | **图里与公式里的变量高亮互相对应** | `formula.js` 里**没有任何**与 DAG 联动的代码 | ❌ 完全没有 |
| 8 | **PC 与手机都能方便阅读** | 手机是**降级**（提示"需要更宽的屏幕"+静态卡片） | ❌ 是降级不是适配 |

**结论：4 条不符、1 条部分、2 条符合、1 条超范围。**

## 试点做法：只改 L00，不碰其余 6 个

新增一个渲染模块（建议 `labs/assets/engine/views/variable-dag.js`），L00 页面用它替代引擎的 `dag.js`。
其余 6 个 lab 一行不动，它们的验收脚本必须继续全过。**试点成立再谈迁移。**

理由：一次改 7 个 lab 风险太大，而且形态还没被你看过。

## ⚠️ 做 demo 时踩到的坑（**这些是重来的成本，务必先读**）

我在 `/tmp` 做了个可跑的示例页验证形态，踩了这些坑。每一条都是引擎里已经解决/有先例的，但不知道就会重踩：

### 1. KaTeX 不吃注入的 HTML —— 锚点恒为 0

想给公式里的变量加高亮，**不能**把 `<span>` 拼进 LaTeX 字符串：KaTeX 把它当文本丢掉。实测锚点数恒为 0。

**正解**：`\htmlId{id}{...}` / `\htmlClass{cls}{...}`，让 KaTeX 自己生成带属性的元素。**且必须配 `trust: true`** —— 不加时 KaTeX **静默**不生成元素（引擎 `formula.js` 的 `katexOpts` 注释专门写了这条）。

### 2. `\region{...}` 的 body 里嵌套 `\slot{}` —— 正则展开必炸

`\region{CORR}{\exp(\slot{MOLD} - \slot{MNEW})}` 的 body 里有 slot，**不递归**就会把字面量 `\slot`/`\regionCORR` 交给 KaTeX，屏幕上打出来。

**正解**：逐字符扫描 + 花括号配对 + 递归（引擎 `formula.js` 的 `expand()` 就是这么写的）。正则处理不了嵌套。

### 3. 公式的值必须**逐 slot 单独渲染**，不能改 textContent

触发替换的**恰恰是符号档**（`\slot{BLKMAX}` → `\max_i x^{(j)}_i`），那本身是 LaTeX，`textContent` 会把反斜杠打到屏幕上。引擎的文件头用了整整两段讲这个坑。

### 4. 分层算法会**跳号** —— 按层号算尺寸会得出荒诞结果

`layer[w] = max(layer[w], base+1)` 累积会产出 `0, 1, 17, 18, 19` 这种层号。按层号直接算画布宽度得到 **4526px**，图被压成一条线。

**正解**：按「实际出现过的层」重编号，再用重编号后的序号算尺寸。

### 5. 横排还是竖排，要按**离目标比例的距离**选，不能比大小

横排 6.2:1、竖排 0.65:1，面板是 1.0:1。**竖排更近**，但 `0.65 < 6.2`，写「谁大选另一个」会选错。

**正解**：对数距离 `|ln(比例/目标)|` 取小者（比例是对称的，2:1 与 1:2 一样偏）。

### 6. `state` 字段要显式带进数据

我第一版忘emit，导致数值查看器读不到值、静默不渲染。L00 的 trace 里 `state` 是**每步都有**的（步 4 有 `acc`）。

### 7. 一屏的关键取舍：**只留 DAG + 公式侧栏**

原来四个面板（DAG + 张量网格 + 公式 + 时间轴）同屏塞不进一屏。示例页的做法是：
- 左/上：DAG
- 右/下：公式侧栏（含 读/写 chips、数值块、narration）
- 底：控制条吸底

实测 PC `900/900`、手机 `844/844`，**零滚动**，且手机是真适配（不是降级）。

## 示例页

`docs/plans/variable-dag-demo.html` —— 可跑的参考实现（自包含，含 L00 真实 trace）。
**注意**：它引用 KaTeX 的路径是预览服务器的（`./AIInfraGuide/labs/assets/vendor/katex/...`），
进仓库要改成 `./assets/vendor/katex/...`。

已验证：公式锚点 5 个（覆盖 `acc`/`m`/`x`）、点图或点公式双向高亮、PC 与手机均一屏、零 console error。

### 一处示例页**未解决**的问题

**公式排版偏松**：数值档把每个 slot 都替成数值，加上 `\phantom` 垫宽残留，长公式会折行、间距不匀。
引擎是按**当前档位**的值垫宽，我没完全对齐。改造时要按引擎的做法处理。

## 数据侧要改什么

**trace 不用变**。`tensors` + `steps[].reads/writes/state` 已经够推导出「变量为节点、计算为边」的图 ——
示例页就是从现有 trace 现推的，没有任何新字段。

唯一要补的是 **`binding.sym` → 变量名**的映射（哪几个张量出现在这条公式里）。
示例页用正则从 `sym` 里提取（`\mathrm{acc}^{(j-1)}` → `acc`），可行但要小心词边界
（`x` 不能命中 `x_blk` 内部）。**更好的做法是让生成器直接输出这个映射**，别在页面里猜。


---

## L00 试点结果（已完成）

新增 `labs/assets/engine/views/variable-dag.{js,css}`，L00 页面改用它。**其余 6 个 lab 一行未动。**

**验收**：`verify-labs.py` 从 76 项涨到 **112 项**（新增的都是新形态的契约断言），其余八个脚本无回归。

### 用户要求的落实

| 要求 | 落实 |
|---|---|
| 一屏 | 每一步一屏。图固定 viewBox 交给 `preserveAspectRatio`；手机不重排图、只上下堆叠；参考面板折进 `<details>`。实测 PC/手机逐步的溢出全为 0 |
| 节点 = 变量 | ✅ 节点集 == `tensors` 的键 |
| 边 = 一次计算 | ✅ `x→x_blk(load) · m→l(sum-exp) · m→acc(fma) · acc→O(div)` |
| 单步进退 | ✅ 含播放 |
| 侧栏 LaTeX | ✅ 复用引擎 `formula.js` |
| 图↔公式联动 | ✅ 点任一处三处同亮（图节点 / 公式 slot / 读写 chips） |
| PC + 手机 | ✅ 都真适配 |

### 三处它自己发现的问题（都值得记）

**1. 三态不是"没写进去"，是"被覆盖了"。**
`render()` 已经把三态 class 写进 SVG，但紧接着 `paintPinned()` 用**空的 `pinned` 集**跑了一遍，`classList.toggle('on', false)` 把刚写的 `on` 打掉了。修法是让 class **只由三态决定**，不被第二条路径覆盖。

**2. 状态属性上的 CSS transition 会造成视觉滞后。**
150ms 淡入好看，但那 150ms 里侧栏已换、图还没换。它是**量出来的**：同一帧里 `stroke-width`（不在 transition 列表）已更新，`stroke` 还停在上一帧。现在有断言专门盯：跳转后立刻读，必须已是新状态。

**3. 它一度画出了错的图（这条最重要）。**
`rank`（层号→序号）与 `depth`（节点→序号）混用，索引全 `undefined`，于是「最上游的读」**静默退化成 `reads[0]`** —— 画出 `x_blk → l` 这种**算法上不存在的扇出**。图看起来画出来了，但画的是错的。

现在有断言盯死：**`l ← m` 和 `acc ← m`，不是 `x_blk`**。这是"这张图是不是这个算法"的分水岭。

### 生成器新增的字段

`step.binding_vars` —— **每个 binding 各自**引用了哪些张量：

```jsonc
"binding_vars": {"J": [], "LOLD": ["l"], "MOLD": ["m"], "MNEW": ["m"],
                 "SUM": ["m", "x"], "LNEW": ["l"]}
```

比我建议的"整条公式一个扁平集合"更好：**per-binding** 才能做到"鼠标放到某个变量上，只亮它引用的那几个"，而不是整条公式相关的全亮。

lint 会校验这个字段，并**单测两个正则必错的 case**：`\ell` 要能点亮名为 `l` 的节点、`x` 不能误伤 `x_blk`。

### 自环是有意的

`m`/`l`/`acc` 三个变量在图上有自环（`reads ∩ writes` 非空）。这是**递推结构的可视化**：`m ← max(m, ·)` 这种"读自己所写"是在跨迭代携带值，不是退化边。按变量画一次、贯穿全程。

### 迁移其余 6 个 lab 时要注意

- 每个 lab 的 DAG 语义不同：L10/L13 是**调度**不是数据流，L01 是**内存层级**。变量图对它们是否合适，要逐个判断。
- 生成器都要补 `binding_vars`。
- 验收脚本都要按新形态重写断言（L00 的那份可作模板）。
